# Moodle instance URL (no trailing slash)
MOODLE_URL=https://your-moodle-site.com

# Port the HTTP server will listen on
PORT=3000

# NOTE: Moodle token is NOT stored here.
# It is passed per-request via the X-Api-Key header.
# In Poke.com: set "API Key" to your Moodle web service token.
